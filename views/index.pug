extends layout

block content
  h1= title
  p Welcome to Rahul Guntu
  button(type="button",class="btn btn-success", onclick="doReduce()") Press Me
  div(id="reduce") Reduce the value 
  a(href='http://localhost:3000/jewerly') Link
  script.
   let count=20
   let change=-1
   function doReduce(){
    count+=change;
    change-=1;
    document.getElementById('reduce').innerHTML=`Count ${count} with change ${change}`
    console.log(count
    )
   }
